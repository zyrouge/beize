# Logical OR

Logical OR operator (`||`) returns `true` if atleast one of two values is `true`.

```title="Syntax"
operand1 || operand2
```

```title="Example"
a || b

true || false
# true

false || false
# false
```
