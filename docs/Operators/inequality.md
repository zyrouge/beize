# Inequality

Inequality operator (`!=`) can be used to find if two values are not equal.

```title="Syntax"
operand1 != operand2
```

```title="Example"
a != b

1 != 2
# true

"Hello" != "Hello"
# false

"Hello" != "World"
# true
```
