import "outre:console" as console;

# fib := fn (n) {
#   if (n <= 1) return 1;
#   return fib(n - 1) + fib(n - 2);
# }

fib := fn (n) return n <= 1 ? 1 : fib(n - 1) + fib(n - 2);

started := DateTime.now().asMilliseconds();
fib(20);
ended := DateTime.now().asMilliseconds();
console.println("Took " + (ended - started).toString() + " ms!");
